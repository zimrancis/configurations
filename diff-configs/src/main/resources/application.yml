spring:
  profiles:
    active: test


app:
  greeting: Hi Zobair!

my:
  love: I Love You ${app.greeting}
  list:
    values: One,Tow,Three

dbValues: "{connectionString: 'https://localhost:8088/dbcon', userName: 'foo', password: 'pass'}"

db:
  connection: {connectionString: 'https://localhost:8088/dbcon', userName: 'foo', password: 'pass'}
  host: 127.0.0.1
  port: 1200

management.endpoints.web.exposure.include: '*'

